import 'package:flutter/material.dart';

class AsVolunteer extends StatelessWidget {
  const AsVolunteer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
